node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
coverage/
.DS_Store
*.log
RESTART.md
.mcp.json
.claude/

# rea is a LOCAL/global developer governance tool — never committed, never in
# CI. The global rea CLI operates on a gitignored .rea/ directory per project.
.rea/
.gitignore.rea-tmp-*
.rea.lock
