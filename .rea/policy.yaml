# .rea/policy.yaml — managed by rea v0.50.0
# Edit carefully: tightening takes effect on next load; loosening requires human approval.
version: "1"
profile: "bst-internal"
installed_by: "himerus"
installed_at: "2026-07-12T13:04:06.581Z"
autonomy_level: L3
max_autonomy_level: L3
promotion_requires_human_approval: true
block_ai_attribution: true
blocked_paths:
  - ".rea/"
  - ".github/workflows/"
  - ".env"
  - ".env.*"
notification_channel: ""
injection:
  suspicious_blocks_writes: true
context_protection:
  delegate_to_subagent:
    - "pnpm run build"
    - "pnpm run test"
    - "pnpm run lint"
  max_bash_output_lines: 100
architecture_review:
  patterns:
    - "src/types/"
    - "src/gateway/"
    - "src/config/"
    - "src/cli/commands/init/"
    - "hooks/_lib/"
    - "templates/"
    - "profiles/"
attribution:
  co_author:
    enabled: false
audit:
  rotation: {}
review:
  codex_required: true
bootstrap_allowlist:
  enabled: true
